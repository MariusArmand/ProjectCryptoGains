name: "CodeQL"
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  schedule:
    - cron: '0 0 * * *'
permissions:
  security-events: write
jobs:
  analyze:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'
      - name: Restore Dependencies
        run: dotnet restore ProjectCryptoGains.sln
      - name: Build
        run: dotnet build ProjectCryptoGains.sln --no-restore --configuration Release
      - name: Check Build Output
        run: dir bin /s /b
        shell: cmd
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: csharp
          build-mode: manual
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
